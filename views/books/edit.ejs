<!DOCTYPE html>
<html>

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/style.css">
  <title>Edit <%= book.title %></title>
</head>

<body>
  <div class="container">
    <%- include('../partials/_navbar.ejs') %>

    <div class="main-content">
      <h1>Edit Book</h1>

      <form action="/books/<%= book._id %>?_method=PUT" method="POST" enctype="multipart/form-data">
        <div class="form-group">
          <label for="title">Title:</label>
          <input type="text" name="title" id="title" value="<%= book.title %>" required>
        </div>

        <div class="form-group">
          <label for="author">Author:</label>
          <input type="text" name="author" id="author" value="<%= book.author %>" required>
        </div>

        <div class="image-input-section">
          <label>Book Cover Image:</label>
          <div class="image-options">
            <div class="upload-option">
              <label for="imageFile">Upload New Image:</label>
              <input type="file" name="imageFile" id="imageFile" accept="image/*">
            </div>
          </div>
        </div>

        <div class="current-image image-container">
          <% if (book.imageUrl) { %>
          <label>Current Image:</label>
          <img src="<%= book.imageUrl %>" alt="Current book cover" class="current-book-cover" onclick="document.getElementById('imageFile').click();">
          <div style="position: absolute; top: 5px; right: 5px; background: rgba(0,0,0,0.5); color: white; padding: 2px 5px; border-radius: 5px; cursor: pointer;" onclick="document.getElementById('imageFile').click();">
            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-pencil" viewBox="0 0 16 16">
              <path d="M11.293 1.293a1 1 0 0 1 1.414 0l2 2a1 1 0 0 1 0 1.414l-9 9a1 1 0 0 1-.39.242l-3 1a1 1 0 0 1-.637-.637l1-3a1 1 0 0 1 .242-.39l9-9zM12 2l2 2-9 9-3 1 1-3 9-9z" />
            </svg>
          </div>
          <% } %>
        </div>

        <div class="form-actions">
          <button type="submit" class="btn-edit">Update Book</button>
          <a href="/books" class="btn-small btn-view">Cancel</a>
        </div>
      </form>
    </div>
  </div>
</body>

</html>